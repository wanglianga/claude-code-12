# 复制为 .env 后按需修改；验证环境由平台注入 CC_PUBLISH_PORT / COMPOSE_PROJECT_NAME

# 宿主机发布端口（应用入口 = 前端 nginx）
CC_PUBLISH_PORT=3012

# PostgreSQL
DB_USER=labuser
DB_PASSWORD=labpass123
DB_NAME=labreagent

# JWT 签名密钥（生产环境务必修改）
JWT_SECRET=please-change-me-in-production

# 是否初始化演示数据（首次启动且用户表为空时生效）
SEED_DEMO=true
